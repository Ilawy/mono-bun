{
  "name": "mono-template",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "template",
    "bun",
    "monorepo"
  ],
  "author": "",
  "license": "ISC",
  "description": "A template for monorepo with bun as the runtime",
  "workspaces": [
    "packages/*"
  ]
}
